/**
 * Native (and Jest) hero dice stage — sync path, no WebGL.
 * Web resolves `HeroDiceStage.web.tsx` instead and idle-loads Three.js.
 */
export { ScatteredCssDice as HeroDiceStage } from "./CssDice3D";
